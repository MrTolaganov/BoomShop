<div
  class="product-image"
  style="background-image: url({{product.image}});"
></div>
<p class="fw-bold fs-1 m-0">{{product.title}}</p>
<div class="d-flex gap-3 align-items-center">
  <small class="text-muted">CreatedAt:
    <b>{{formatDate product.createdAt}}</b>
  </small>
</div>
<p>{{product.description}}</p>
<div class="d-flex justify-content-end">
  <p class="fw-bold fs-3 me-2">{{product.user.firstname}}</p>
  <p class="fw-bold fs-3">{{product.user.lastname}}</p>
</div>